/**
 * 00 Create and setup project
 * 
 * Install React Router using the following command:
 * npm install react-router-dom
 * 
 * In your React project, import BrowserRouter, Route, Routes, and Link 
 * from 'react-router-dom' in the main App.js file
 * 
 */

/**
 * 01 Create a simple navigation structure
 * 
 * Create three new components called Home, About, and Contact in a React project.
 * In each component, add a simple heading or text describing the component 
 * (e.g., "Welcome to the Home page").
 * In your App.js file, use the BrowserRouter component to wrap your application.
 * Inside the BrowserRouter, use the Link component to create navigation links for Home, About, and 
 * Contact pages.
 * 
 */

/**
 * 02 Create a simple navigation structure
 * 
 * Create three new components called Home, About, and Contact in your React project.
 * In each component, add a simple heading or text describing the component 
 * (e.g., "Welcome to the Home page").
 * In your App.js file, use the BrowserRouter component to wrap your application.
 * Inside the BrowserRouter, use the Link component to create navigation links for 
 * Home, About, and Contact pages.
 * 
 */

/**
 * 03 Configure routes
 * 
 * In your App.js file, use the Routes component to wrap the Route components.
 * Create three Route components, one for each of the Home, About, and Contact pages. 
 * Set the 'path' prop for each Route component to the appropriate URL path 
 * (e.g., '/', '/about', '/contact').
 * Set the 'component' prop of each Route component to the corresponding component (e.g., Home, About, Contact).
 * Add the 'exact' prop to the Home route to ensure it only matches when the path is exactly '/'.
 *  
 */



/**
 * 04 Test your navigation
 * 
 * Start your React application and navigate to the Home, About, and Contact pages using
 * the links you created in Exercise 2.
 * Verify that the correct components are rendered when you click on each link.
 * Test the browser's back and forward navigation buttons to ensure they work as expected.
 * 
 */