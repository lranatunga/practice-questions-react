export default function ToggleText (displayText) {
    return(
        <div>Hello, World!</div>
    // displayText === true ? <div>Hello, World!</div> : null

 )
}










/**
 * 02 Toggle Text Display:
 * 
 * Create a component called ToggleText that accepts a prop called displayText. 
 * The component should display the text "Hello, World!" when displayText is true, and display nothing when displayText is false.
 * 
 */